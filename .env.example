# Environment variables for local development.
# Copy to .env and adjust values:  cp .env.example .env

# Database connection string
DATABASE_URL=sqlite:////tmp/app.db
# Secret key for JWT token signing
JWT_SECRET_KEY=dev-secret-change-in-production
# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# Read by the backend code (STRIPE_PUBLISHABLE_KEY)
STRIPE_PUBLISHABLE_KEY=pk_test_mock_pub_key
# Read by the backend code (STRIPE_SECRET_KEY)
STRIPE_SECRET_KEY=sk_test_mock_secret_key
# Read by the backend code (STRIPE_WEBHOOK_SECRET)
STRIPE_WEBHOOK_SECRET=whsec_mock_webhook_secret
# Read by the backend code (TESTING)
TESTING=false
